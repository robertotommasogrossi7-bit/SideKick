# Prompt — migrazione HABIT, arm-B (SENZA pacchetto)
> Incollalo come **primo messaggio** in una chat Claude Code pulita con root =
> `Programmi/_migr-test/habit-armB`. Poi lasciala lavorare da sola, senza aiuti.

---

In questa cartella c'è un'app **vanilla HTML + CSS + JavaScript** funzionante: un **tracker di
abitudini** (crei abitudini con una data d'inizio; registri "occorrenze"/completamenti con note
opzionali; vedi la **streak** corrente e il record; badge ai traguardi di durata; import/export
JSON). Il frontend sta in `web/`. I dati stanno in **localStorage**. La cartella è un repo git
con un commit iniziale (l'app originale su `main`).

Lavora **esclusivamente** dentro questa cartella: non leggere né aprire file di altre cartelle
del computer.

Compito: **migra questa app a React + Vite (TypeScript)**. L'app migrata deve **funzionare**:
build di produzione verde e app che gira. Lavora con cura e committa man mano.

Quando hai finito, raccontami: cosa hai fatto e in che ordine, e come verifico che funziona.
