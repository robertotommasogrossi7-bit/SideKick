# Images for the README

- `DASHBOARD.png` — the "At a glance" + "Most expensive things" blocks of
  `observatory/usage/DASHBOARD.md` as rendered by GitHub.
- `WTB.png` — the sessions table of
  `observatory/usage/per-project/poker-who-s-the-boss.md` (a real app's full build
  history, session by session).

These must stay committed (not gitignored): GitHub can only render images that live in
the repo. They contain only already-published, redacted data.
