# Immagini per il README

- `DASHBOARD.png` — i blocchi "A colpo d'occhio" + "Le cose più costose" di
  `observatory/usage/DASHBOARD.md` come renderizzati da GitHub.
- `WTB.png` — la tabella delle sessioni di
  `observatory/usage/per-project/poker-who-s-the-boss.md` (la cronologia di build completa
  di un'app reale, sessione per sessione).

Queste devono restare committate (non gitignorate): GitHub può renderizzare solo immagini
che vivono nel repo. Contengono solo dati già pubblicati e redatti.
