# SideKick — il PROCESSO è pubblico apposta (si condivide), quindi _processo/ NON è ignorato.

# Config locale di Claude Code / agenti (può contenere credenziali/token)
/.claude/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
dist/

# OS
.DS_Store
Thumbs.db

# Artefatti transitori del motore di distillazione
_PROMPT_PRONTO.md

# Archivio locale del materiale storico (ex versione-italiano/, decisione Roberto 2026-07-25):
# mai su GitHub, gli originali restano nella cronologia git. La versione italiana e' ITALIANO/.
old/

# Compiti permanenti dell'osservatorio (operativo, solo locale)
/OSSERVATORIO.md

# Banco di prova (copia di app per testare SideKick) - non versionato
/_test/

# Legenda della censura dei progetti privati (SOLO locale, mai su GitHub)
observatory/censura.local.json

# (glossario personale: spostato nel repo Studio il 2026-07-25, gitignorato la')
