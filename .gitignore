# SideKick — the PROCESS is public on purpose (it gets shared); the historical `_processo/`
# folders now live only in the local `old/` archive and in git history.

# Local Claude Code / agent config (may contain credentials/tokens)
/.claude/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
dist/

# OS
.DS_Store
Thumbs.db

# Transient artifacts of the distillation engine
_PROMPT_PRONTO.md

# Local archive of the historical material (ex versione-italiano/, Roberto's decision
# 2026-07-25): never on GitHub; the originals remain in git history. The Italian tree is ITALIANO/.
old/

# The observatory's standing tasks (operational, local only)
/OSSERVATORIO.md

# Test bench (app copies used to test SideKick) - not versioned
/_test/

# Redaction legend for private projects (LOCAL only, never on GitHub)
observatory/censura.local.json

# Local-only registries (positions of shared numbers/versions, etc.) — one file per
# concern, named *.local.md, kept out of the public repo by design
*.local.md

# (personal glossary: moved to the Studio repo on 2026-07-25, gitignored there)
