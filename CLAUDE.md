# SideKick — istruzioni per Claude Code

> Caricato in automatico quando lavori in questa cartella. Il **metodo** completo è in
> `plugins/metodo/COSTITUZIONE.md` (identico alla copia attiva `~/.claude/CLAUDE.md`).

**All'avvio, leggi `OSSERVATORIO.md`**: elenca i compiti permanenti di SideKick (aggiornare il
`glossario/` dai materiali di studio AWS, esperimenti sul metodo, ecc.).

Punti d'ingresso:
- `OSSERVATORIO.md` — compiti permanenti + ruolo di SideKick (ottimizzare/testare/analizzare Claude Code).
- `glossario/INDICE.md` — i termini che Roberto sta imparando (dove finiscono i termini nuovi).
- `GUIDA.md` — come distillare/usare le feature della `libreria/`.
- `plugins/metodo/COSTITUZIONE.md` — il metodo di lavoro canonico.
